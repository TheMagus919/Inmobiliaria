@model List<proyecto.Models.Inquilino>

@{
    ViewData["Title"] = "Index";
}

<h1>Inquilino</h1>

<p>
    <a asp-action="Create">Crear Nuevo Inquilino</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <h3>IdInquilino</h3>
            </th>
            <th>
                <h3>Nombre</h3>
            </th>
            <th>
                <h3>Apellido</h3>
            </th>
            <th>
                <h3>Dni</h3>
            </th>
            <th>
                <h3>Telefono</h3>
            </th>
            <th>
                <h3>Email</h3>
            </th>
            <th>
                <h3>Opciones</h3>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdInquilino)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dni)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id=item.IdInquilino }) |
                @Html.ActionLink("Detalles", "Details", new { id=item.IdInquilino }) |
                @Html.ActionLink("Eliminar", "Delete", new { id=item.IdInquilino })
            </td>
        </tr>
}
    </tbody>
</table>
